Make sure you're on the correct node version from `~/holiday_acres/.nvmrc`

```bash
# install packages
$ yarn
# hot-reloaded dev ui
$ yarn dev
```

Build production bundle, run production bundle

```bash
$ yarn build
$ yarn start
```
